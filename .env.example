# Docker's host port; Portless forwards to this port.
PROXY_PORT=18081
# Directory containing workbuddy-desktop-ai.info. Defaults to the macOS WorkBuddy AI auth directory.
# WORKBUDDY_AUTH_DIR=/absolute/path/to/auth-directory
